-- ==========================================
-- 用途: マイグレーション再実行前のテーブルクリア
-- Created: 2026-04-23
-- Reason:
--   前回の本番マイグレーションで x_username 重複により500件が失敗（残り746件は成功）。
--   スクリプトで重複排除ロジックを追加したので、テーブルをクリアしてから再実行する。
-- ==========================================

-- illustrators テーブルを空にする（sync_failures への FK がある場合は CASCADE で連動）
TRUNCATE TABLE illustrators RESTART IDENTITY CASCADE;
